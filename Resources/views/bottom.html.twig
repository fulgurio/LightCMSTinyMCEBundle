<script src="{{ asset('bundles/fulguriolightcmstinymce/js/jquery.tinymce.js') }}"></script>
<script>
$(function() {
	$('{{ idElt }}').tinymce({
		script_url : '{{ asset('bundles/fulguriolightcmstinymce/js/tiny_mce.js') }}',
		popup_css: false,
		language : "fr",
{% if config.content_css is defined %}		content_css: '{{ asset(config.content_css) }}',{% endif %}
		height : "400",
		theme : "{{ config.theme }}",
		skin : "{{ config.skin }}",
		plugins : "{{ config.plugins }}",
		theme_advanced_buttons1 : "{{ config.theme_advanced_buttons1 }}",
		theme_advanced_buttons2 : "{{ config.theme_advanced_buttons2 }}",
		theme_advanced_buttons3 : "{{ config.theme_advanced_buttons3 }}",
		theme_advanced_toolbar_location : "top",
		theme_advanced_toolbar_align : "left",
		theme_advanced_statusbar_location : "bottom",
		theme_advanced_resizing : true,
		theme_advanced_resize_horizontal: false,
		file_browser_callback: 'filesBrowser',
		uploadFile: {
			title: "",
			url: "{{ path('AdminMediasWysiwyg') }}",
			width: $(document).width() * 0.9,
			height: $(document).height() * 0.8,
		},
		link: {
			title: "",
			url: "{{ path('AdminLinkWysiwyg') }}",
			width: $(document).width() * 0.9,
			height: $(document).height() * 0.8,
		},
	});
});
</script>